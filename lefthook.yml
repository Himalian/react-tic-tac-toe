# Documentation: https://lefthook.dev/configuration/
pre-commit:
  parallel: true
  jobs:
    - run: bun run biome format --write {staged_files}
      glob: "*.{js,ts,jsx,tsx}"
    - run: bun run biome lint --write {staged_files}
      glob: "*.{js,ts,jsx,tsx}"
